<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>$(AssemblyName)</Title>
    <Authors>SKitLs, Sargeras02</Authors>
    <Description>SKitLs.Data.Core is a powerful C# library designed to streamline data management.
It offers a unified interface for handling data, ensuring automatic data persistence and real-time updates through event notifications.</Description>
    <Copyright>Copyright (C) 2024, SKitLs</Copyright>
    <PackageProjectUrl>https://github.com/SKitLs-dev/SKitLs.Data.Core</PackageProjectUrl>
    <PackageIcon>sk.dt.core.png</PackageIcon>
    <PackageTags>data-management</PackageTags>
    <PackageReleaseNotes>DataBanks:
(!) Fixed: DataBanks now reads data on Initilize()
(!) Fixed: DataBanks now saves objects properly (since .Data.IO v.2.0.0 release)
(!) Fixed: DataBanks now drops objects properly on Delete strategy
(~) Updated: BuildNewData() now overrides Id to a default one
(~) Updated: DayaBankInfo now is a class
(~) Updated: Count now returns only available count
(+) Added: CountAll property for getting all values count

IdGens:
(~) Fixed: SolidLongIdGen new id retrieving for DropStrategy = Disable
(+) Added: GuidIdGenrator

(~) Minor updates</PackageReleaseNotes>
    <AssemblyVersion>1.0.0</AssemblyVersion>
    <FileVersion>$(AssemblyVersion)</FileVersion>
    <Version>$(AssemblyVersion)</Version>
    <IncludeSymbols>False</IncludeSymbols>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>True</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <GenerateDocumentationFile>True</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\sk.dt.core.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SKitLs.Data.IO" Version="2.0.1" />
    <PackageReference Include="SKitLs.Utils.Extensions" Version="0.2.0" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
